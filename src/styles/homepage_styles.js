import { makeStyles } from "@material-ui/styles";

const useStylesHomePage = makeStyles({
  homePageContent: {
    padding: "10px",
    width: "100%",
    height: "70vh",
    margin: "20px",
    display: "flex",
    justifyContent: "center",
  },
});

export { useStylesHomePage }; 